name: Publish Docker image when a release is published

on:
  release:
    types: [published]

jobs:
  push_to_registry:
    name: Push Docker image to GitHub Packages
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello world -- build and push the container image to a container registry"
  
  deploy_container:
    name: Deploy container
    runs-on: ubuntu-latest
    needs: push_to_registry
    steps:
      - run: echo "Hello world -- deploy the newly published container image to a hosting service"
